# 🤖 Autonomous Agent Tasks

> **Operational Directives:**
> 1. **Semantic Domain Matching:** Scan the board for your specific Archetype or Mechanical Verb. If a task matches, claim it.
> 2. **Silent Rejection:** If you review this board and find ZERO tasks that match your specific domain, DO NOT mark out-of-scope tasks as "Blocked". Initiate your own native discovery scan across the repository.
> 3. **Atomic Deletion:** Do not mark items as resolved `[x]`. Purely delete the exact line of the task you have completed to establish a zero-waste context loop.

## The [OPERATOR] Queue
* `package-lock.json`: Lockfile mismatch detected against package.json. Sync required.

## The [REFACTORER] Queue
* `bookmarklets/pa-county-finder.js`: 1653 lines. Requires domain splitting and colocation.
* `bookmarklets/property-clipper.js`: 1088 lines. Requires domain splitting and colocation.
* `mailto-link-generator/js/mailto.js`: 987 lines. Requires domain splitting and colocation.
* `mailto-link-generator/js/msgreader.js`: 887 lines. Requires domain splitting and colocation.
* `bookmarklets/utils.js`: 838 lines. Requires domain splitting and colocation.
* `bookmarklets/web-clipper.js`: 690 lines. Requires domain splitting and colocation.
* `bookmarklets/macro-builder.js`: 528 lines. Requires domain splitting and colocation.
* `bookmarklets/passphrase-generator.js`: 506 lines. Requires domain splitting and colocation.

## The [PRUNER] Queue
* `bookmarklets/pa-county-finder.js:1306`: Remove fossilized commented debris.
* `bookmarklets/utils.js:683`: Excise fossilized warning block.
* `mailto-link-generator/js/msgreader.js:697`: Remove fossilized warning block.

## The [INSTRUMENTER] Queue
* `bookmarklets/job-clipper.js:33`: Replace empty catch block `catch { /* ignore */ }` with explicit error telemetry.
* `bookmarklets/job-clipper.js:231`: Replace empty catch block `catch { /* ignore */ }` with explicit error telemetry.

## The [TRANSFORMER] Queue
* `bookmarklets/pa-county-finder.js:1401`: Replace hardcoded hex code (`#2563eb`) with a dynamic design token.
